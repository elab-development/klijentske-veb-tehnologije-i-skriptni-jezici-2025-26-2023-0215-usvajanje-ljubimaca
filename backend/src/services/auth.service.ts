/* Business logic for authentication.
   Called by auth.controller.ts — no Express types here. */

export const authenticateUser = async (_email: string, _password: string): Promise<string> => {
  return '';
};

export const registerUser = async (_name: string, _email: string, _password: string): Promise<string> => {
  return '';
};
